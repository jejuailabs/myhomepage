# 조천리 부녀회 Heroes — 미니홈피 웹빌더

## 프로젝트 개요
개별 유저(마을 부녀회원 등)가 **텍스트 + 참조이미지 + mp3**를 입력하면, 6종의 디자인 컨셉 중 하나를 선택해
**본인만의 모바일 전용 미니 홈페이지**가 자동 생성되는 커뮤니티 웹빌더.
전체 유저의 홈페이지는 메인 허브 화면에서 **세로형 히어로 카드 캐러셀**로 모여 보여진다.

이 문서는 프로젝트 전체를 관통하는 마스터 스펙이며, 상세 내용은 `docs/` 폴더의 개별 문서를 참조한다.
Claude Code는 반드시 아래 `docs/` 파일들을 전부 읽은 뒤 작업을 시작할 것.

- `docs/01_information_architecture.md` — 메인 허브 + 개별 홈피 화면/섹션 구조
- `docs/02_design_concepts.md` — 6종 디자인 컨셉 (팔레트/레이아웃/톤)
- `docs/03_data_model.md` — Firestore 데이터 모델
- `docs/04_tech_and_features.md` — 기술 스택, mp3 자동재생 처리, 관리자 모드

## 핵심 컨셉 요약
1. **메인 허브**: 상단 헤더 "조천리 부녀회 Heroes" (영문 표기). 본문은 좌우 스와이프되는
   세로형 히어로 카드 리스트 — 이미지 비중 80%+, 하단에 인물 요약 1~2문장. 카드 클릭 시 개별 홈피로 이동.
2. **개별 홈피**: 모바일 전용 1페이지형. 6종 디자인 컨셉 중 제작 시 선택한 1종의
   레이아웃/컬러 팔레트를 그대로 따르되, 내용(텍스트/이미지/mp3)만 유저 데이터로 치환.
3. **mp3**: 개별 홈피 진입 시 자동재생. 단, 브라우저 자동재생 정책 대응 로직 필수 (04번 문서 참조).
4. **베이스는 무조건 모바일 우선(mobile-first)** — 데스크톱 대응은 우선순위 낮음(있으면 중앙 정렬 모바일 프레임 형태로만 확장).

## 기술 스택 (기본값)
- Next.js + Vercel 배포
- Firebase: Firestore (데이터), Auth (Google 로그인), Storage (이미지/mp3 업로드), Cloud Functions
- Tailwind CSS
- next-intl (i18n) — 우선순위 낮음, 구조만 대비
- 다크/라이트 모드 토글 — 이 프로젝트는 각 홈피가 고유 컨셉 팔레트를 가지므로 **다크/라이트 토글은 메인 허브에만 적용**,
  개별 홈피는 선택된 컨셉의 고정 팔레트를 유지 (토글 비적용). ⚠️ 확인 필요 항목 (04번 문서 참조)
- 관리자 모드: 기본 포함 (부녀회 운영진이 유저 승인/노출순서/카드 정렬 관리)

## 작업 원칙
- 6종 디자인 컨셉은 "레이아웃 + 컬러 팔레트"를 원본 이미지와 최대한 근접하게 재현하는 것이 최우선 목표.
  각 컨셉 내부에서 바뀌는 것은 콘텐츠(사진/텍스트/음악)뿐, 구조와 톤은 고정.
- 신규 유저 입력 데이터가 컨셉의 섹션 구조(취향/꿈과 도전/강점/사랑하는 것/행복한 순간)에
  자연스럽게 매핑되도록 입력 폼 설계.
- HTML5 시맨틱 마크업 + Tailwind로 각 컨셉의 시각적 디테일(카드 그림자, 폴라로이드 느낌, 종이 질감 등)을
  최대한 CSS로 구현. 이미지 텍스처가 필요한 경우 참조이미지 업로드 슬롯으로 대체.
